spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  elasticsearch:
    uris: http://localhost:9200
  data:
    redis:
      host: localhost
      port: 6379

storage:
  datasource:
    core:
      driver-class-name: com.mysql.cj.jdbc.Driver
      jdbc-url: jdbc:mysql://localhost:3306/elasticsearchdb?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
      username: jonghyun-mysql
      password: jonghyun-mysql
      maximum-pool-size: 10
